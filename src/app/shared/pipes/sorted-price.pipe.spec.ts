import { SortedPricePipe } from './sorted-price.pipe';

describe('SortedPricePipe', () => {
  it('create an instance', () => {
    const pipe = new SortedPricePipe();
    expect(pipe).toBeTruthy();
  });
});
