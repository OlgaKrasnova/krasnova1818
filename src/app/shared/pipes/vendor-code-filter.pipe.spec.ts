import { FilterVendorCodePipe } from './vendor-code-filter.pipe';

describe('FilterVendorCodePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterVendorCodePipe();
    expect(pipe).toBeTruthy();
  });
});
